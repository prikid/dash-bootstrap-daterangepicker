# AUTO GENERATED FILE - DO NOT EDIT

''DashBootstrapDaterangepicker <- function(id=NULL, className=NULL, end_date=NULL, initialSettings=NULL, innerClassName=NULL, start_date=NULL) {
    
    props <- list(id=id, className=className, end_date=end_date, initialSettings=initialSettings, innerClassName=innerClassName, start_date=start_date)
    if (length(props) > 0) {
        props <- props[!vapply(props, is.null, logical(1))]
    }
    component <- list(
        props = props,
        type = 'DashBootstrapDaterangepicker',
        namespace = 'dash_bootstrap_daterangepicker',
        propNames = c('id', 'className', 'end_date', 'initialSettings', 'innerClassName', 'start_date'),
        package = 'dashBootstrapDaterangepicker'
        )

    structure(component, class = c('dash_component', 'list'))
}
